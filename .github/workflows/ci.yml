name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: "1.27.x"
  GOFLAGS: "-mod=readonly"

jobs:
  static-guards:
    name: Static guards
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: Verify formatting
        shell: bash
        run: |
          unformatted="$(gofmt -l $(find . -name '*.go' -not -path './.git/*'))"
          if [ -n "$unformatted" ]; then
            printf 'Unformatted Go files:\n%s\n' "$unformatted"
            exit 1
          fi
      - name: Verify module state
        shell: bash
        run: |
          go mod verify
          go mod tidy -diff
          go list -m all
          while IFS= read -r module; do
            case "$module" in
              github.com/modelcontextprotocol/go-sdk|go.yaml.in/yaml/v3|github.com/SivanCola/windows-sandbox|github.com/google/jsonschema-go|github.com/segmentio/asm|github.com/segmentio/encoding|github.com/yosida95/uritemplate/v3|github.com/golang-jwt/jwt/v5|github.com/google/go-cmp|cloud.google.com/go/compute/metadata|golang.org/x/oauth2|golang.org/x/sync|golang.org/x/sys|golang.org/x/time|golang.org/x/tools)
                ;;
              *)
                printf 'Unapproved Go module: %s\n' "$module"
                exit 1
                ;;
            esac
          done < <(go list -m -f '{{.Path}}' all | tail -n +2)
      - name: Enforce CGO and L1 dependency boundaries
        shell: bash
        run: |
          if find . -name '*.go' -not -path './.git/*' -print0 | xargs -0 grep -nE '(^|[[:space:]])import[[:space:]]+(_[[:space:]]+)?"C"|//[[:space:]]*#cgo'; then
            printf 'CGO usage is forbidden.\n'
            exit 1
          fi
          nonstandard="$(go list -deps -f '{{if and (not .Standard) (not .Module)}}{{.ImportPath}}{{end}}' ./internal/core)"
          if [ -n "$nonstandard" ]; then
            printf 'L1 imports non-standard packages:\n%s\n' "$nonstandard"
            exit 1
          fi
      - name: Run architecture guard tests
        env:
          CGO_ENABLED: "0"
        run: go test -count=1 ./internal/core

  native-tests:
    name: Native tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            sandbox_coverage_floor: "80"
          - os: macos-latest
            sandbox_coverage_floor: "80"
          - os: windows-latest
            sandbox_coverage_floor: "90"
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: Install bubblewrap
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends bubblewrap
          bwrap --version
      - name: Test
        env:
          CGO_ENABLED: "0"
        run: go test -count=1 ./...
      - name: Test platform sandbox suites verbosely
        env:
          CGO_ENABLED: "0"
          MARXAGENT_REQUIRE_SANDBOX: "1"
        run: go test -count=1 -v -run 'Test(Windows|Linux|Darwin|SandboxBackend)' ./internal/engine/sandbox
      - name: Vet
        env:
          CGO_ENABLED: "0"
        run: go vet ./...
      - name: Enforce sandbox coverage floor
        env:
          CGO_ENABLED: "0"
        shell: bash
        run: |
          go test -count=1 -coverprofile=sandbox-coverage.out ./internal/engine/sandbox
          go tool cover -func=sandbox-coverage.out
          coverage="$(go tool cover -func=sandbox-coverage.out | awk '/^total:/ {gsub("%", "", $3); print $3}')"
          floor="${{ matrix.sandbox_coverage_floor }}"
          awk -v coverage="$coverage" -v floor="$floor" 'BEGIN { if (coverage + 0 < floor + 0) exit 1 }'
      - name: Upload sandbox coverage profile
        uses: actions/upload-artifact@v4
        with:
          name: sandbox-coverage-${{ matrix.os }}
          path: sandbox-coverage.out
          if-no-files-found: error

  cross-builds:
    name: Build ${{ matrix.goos }}/${{ matrix.goarch }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
          - goos: windows
            goarch: arm64
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: Build without CGO
        env:
          CGO_ENABLED: "0"
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: go build ./...

  race:
    name: Race detector
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: Install bubblewrap
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends bubblewrap
          bwrap --version
      - name: Run race detector
        env:
          CGO_ENABLED: "1"
        run: go test -race -count=1 ./...

  coverage:
    name: L1 coverage
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: Enforce 100 percent statement coverage
        env:
          CGO_ENABLED: "0"
        shell: bash
        run: |
          go test -count=1 -coverprofile=coverage.out ./internal/core
          go tool cover -func=coverage.out
          coverage="$(go tool cover -func=coverage.out | awk '/^total:/ {gsub("%", "", $3); print $3}')"
          awk -v coverage="$coverage" 'BEGIN { if (coverage + 0 < 100.0) exit 1 }'

  performance:
    name: L1 performance gates
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: Enforce latency and memory gates
        env:
          CGO_ENABLED: "0"
          MARXAGENT_PERFORMANCE: "1"
        shell: bash
        run: |
          for gate in 1000_message_snapshot empty_loop 10_hook_trigger 10k_message_agent_memory event_dispatch_p99_with_100_subscribers; do
            go test -run "^TestL1PerformanceGates$/${gate}$" -count=3 ./internal/core
          done
      - name: Record benchmarks
        env:
          CGO_ENABLED: "0"
        shell: bash
        run: |
          go test -run '^$' -benchtime=10000x -count=1 -bench '^BenchmarkEventBusPublish100Subscribers$' ./internal/core
          go test -run '^$' -benchtime=500ms -count=1 -bench 'Benchmark(AgentEmptyLoop|StateSnapshot1000Messages|HookTrigger10Hooks|AgentMemory10000Messages)$' ./internal/core
